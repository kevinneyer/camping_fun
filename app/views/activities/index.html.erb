<h1>All The Activities</h1>

<ul>
  <% @activities.each do |activity| %>
    <li><%= link_to activity.name, activity %></li>
  <% end %>
</ul>

<%= button_to "SEE ALL CAMPERS", campers_path, method: :get %>